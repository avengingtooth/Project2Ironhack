{{!-- results for search by tags and titles --}}
{{>searchOptions this}}
{{#each queryResults}}
    <div class="post" data-id="{{_id}}" data-author="{{author._id}}">
        {{log this}}
        {{> post post=this follows=../follows likes=../likes}}
    </div>
{{/each}}
<h2>Your own profile:</h2>

<p>
    <img src="{{user.profilePictureURL}}" alt="Profile Picture of {{user.username}}">
    username: {{user.username}}
    first name: {{user.firstName}}
    last name: {{user.lastName}}
</p>

<p>
    email: {{user.email}}
</p>


<form action="" method="POST">
    <h3>Edit your data</h3>
    <div>
        <label for="username">Username:</label>
        <input name="username" type="text" value="{{user.username}}">
    </div>
    <div>
        <label for="email">Email:</label>
        <input name="email" type="email" value="{{user.email}}">
    </div>
    <div>
        <label for="firstName">First Name:</label>
        <input name="firstName" type="text" value="{{user.firstName}}">
    </div>
    <div>
        <label for="lastName">Last Name:</label>
        <input name="lastName" type="text" value="{{user.lastName}}">
    </div>
    <div>
        <label for="imageUrl">Profile Picture:</label>
        <input name="imageUrl" type="text" value="{{user.profilePictureURL}}">
    </div>
    <div>
        <label for="password">New Password</label>
        <input type="password" name="password">
        <label for="passwordConfirmation">New Password (repeat)</label>
        <input type="password" name="passwordConfirmation">
    </div>
    <button>Save</button>
</form>